{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd ~/.claude && if ! git diff --quiet || ! git diff --cached --quiet || [ -n \"$(git ls-files --others --exclude-standard)\" ]; then git add -A && git diff --cached --quiet || (git commit -m \"Auto-sync: $(date +%Y-%m-%dT%H:%M:%S)\" && git push origin main 2>&1); fi; exit 0",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "advisorModel": "opus",
  "theme": "dark",
  "tui": "fullscreen",
  "agentPushNotifEnabled": true,
  "skipAutoPermissionPrompt": true,
  "permissions": {
    "defaultMode": "auto"
  },
  "enabledPlugins": {
    "mattpocock-skills@claude-plugins-official": true
  }
}
